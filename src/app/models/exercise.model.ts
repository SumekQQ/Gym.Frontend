export class Exercise{
    public id: number;
    public name: string;
    public categoryId: number;
    public isDefault: boolean;
    
    // constructor(id: number, name: string,categoryId: number, isDefault: boolean){
    //     this.id = id;
    //     this.name = name;
    //     this.categoryId = categoryId;
    //     this.isDefault = isDefault;
    // };
}